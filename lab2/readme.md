# EventLoop

js is synchronous and single threaded bydefault

## there can be async behaviour

-with BrowserAPT-
setTimeout, setInterval, setImmediate, nextTick
-with promises
-with event handlers

# callback

callback function=> that pass as argument or the parameter to another function 
modern javascript is divided into two categories(cjs)

fs module directly communicate to os rather than browser the common operation on the files are 
1. file=> right File, read File, append File
2. Folder=